# cluster.yaml - cluster configuration for Snakefile.rnaseq
__default__:
    cluster: notchpeak
    partition: notchpeak-shared
    account: chpc
    time: 30:00
    ntasks: 1

align_one_sample:
    ntasks: 4
